#function IndividualCoralPieces
Accepts the argument teamNumber
Makes a spreadsheet and pieceSheet then gets the pieceData
Gets the auto and teleop information for the team number and makes variables for each
Checks if values are numeric and sets them to 0 if they are not 
Makes an array of the information, which also has the teamNumber for identification (makes the array with auto and teleop levels) and returns the Array created

#function LowestTeam
Creates a function LowestTeam which takes 4 parameters : the teams and a type(sorting type like Points).
Gets the spreadsheet.
Creates a total points for each team
It then finds the minimum and based on that returns the minimum

#function OrganizeCoral
Creates the spreadsheet variable which is the file
Creates predictionSheet variable which is the "Game Predictor" sheet
Creates OrganizeCoral function which intakes allianceColor
Gets the values of the necessary L4 values from the Prediction Sheet
Makes a variable that assigns a boolean to a certain color
Then sets the variable based on the allianceColor
Gets the piece for each team (individial coral pieces for each team)
Makes a named variable and sets them to each of the values received 
Gets the defense data from the predictionSheet
Finds the team with the lowest pieces
Logs the lowest team found
Subtracts the data of the lowest team from the teleop values and sets them
Creates total level values which adds the auto level values and the teleop values
Sets the limits on the maximum for each level (12,12,12,36)
Gets either max auto or highest level depending on how we prioritize point scoring
Calculates the Auto and Teleop while following the (theoretical) maximum which was made above
Gets teleop again by subtracting auto from the total
Makes another array using the total, auto, and teleop for all 4 levels
Gets the rounded piece
Logs the rounded piece